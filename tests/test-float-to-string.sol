/*@author: Kunal Baweja*/

func main() {
    float f;
    string s;

    f = -10.0;
    s = floatToString(f);
    consolePrint(s);

    f = 0.0;
    s = floatToString(f);
    consolePrint(s);

    f = 10.0;
    s = floatToString(f);
    consolePrint(s);
}